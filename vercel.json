{
  "builds": [
    {
      "src": "api/index.ts",
      "use": "vercel-deno",
      "config": {
        "includeFiles": ["_fresh/**", "static/**"]
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.ts"
    }
  ]
}