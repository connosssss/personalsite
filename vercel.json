{
  "builds": [
    {
      "src": "api/index.ts",
      "use": "vercel-deno",
      "config": {
        "includeFiles": ["_fresh/**", "static/**", "components/**", "islands/**", "routes/**"]
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.ts"
    }
  ]
}